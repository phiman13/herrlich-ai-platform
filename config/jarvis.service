[Unit]
Description=Jarvis Bot Gateway
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/agents
EnvironmentFile=/root/.env
ExecStart=/root/agents/venv/bin/uvicorn main:app --host 0.0.0.0 --port 9000
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
